# apprunner.yaml
version: 1.0
# App Runner のマネージド Python ランタイムを使う（Python 3.11）
runtime: python311

build:
  commands:
    build:
      - pip3 install -r requirements.txt

run:
  runtime-version: 3.11
  command: gunicorn --bind 0.0.0.0:8080 app:app
  network:
    port: 8080
